<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <header>
      <h1><%=restaurant.name%></h1>
      <nav>
        <a href="/undertheradar"><button>Home</button</a>
        <a href="/undertheradar/<%=restaurant.neighborhood%>"><button>Back to Neighborhood</button></a>
        <a href="/undertheradar/<%=restaurant._id%>/edit"><button>Edit</button></a>
        <a href="#"><button>Delete</button></a>
      </nav>
    </header>
    <main>
      Cuisine: <%=restaurant.cuisine%> <br/>
      Rating:
      <% if (restaurant.rating === 0) { %>
        None <br/>
      <% } else { %>
        <%=restaurant.rating%> <br/>
      <% } %>
      Notes: <%=restaurant.comments%> <br/>
      <% if(restaurant.wishList === true) { %>
        <button><a href="#">Check Off Wish List</a></button>
      <% } %>
    </main>
  </body>
</html>
